import React from 'react';
import {
  View,
  TextInput,
  Dimensions,
  Pressable,
  TouchableOpacity,
  Image,
  FlatList,
} from 'react-native';
import POSTS from '../../data/Post';
import { data } from './Search';

const AddStory=()=>{
    return(
      data.map(
        
      )
    )
}

export default AddStory;